*** Settings ***
Documentation     封装DeptDDTree控件的一些操作
Library           Selenium2Library
Resource          通用控件封装_下拉树基类.txt

*** Keywords ***
Map_DeptDDTree_设置组织机构
    [Arguments]    ${deptPath}    ${locator}=xpath=//table[@class='DeptDDTree']
    [Documentation]    *功能：*
    ...
    ...    设置组织机构选项
    ...
    ...    *参数说明：*
    ...
    ...    deptPath : 组织机构的路径，父路径与子路径用/分割
    ...
    ...    locator:
    ...    DeptDDTree控件对应的table元素的定位信息，此参数一般可以省略，
    ...    但是如果定位信息与默认值不同时，则必须填写此参数
    ...
    ...
    ...    *示例：*
    ...
    ...    Map_DeptDDTree_设置组织机构 | 深圳明源/财务部
    Return From Keyword If    '${deptPath}'=='' or '${deptPath}'=='[NULL]'
    #检查上下文
    _myExecuteJS    var tables=window.document.getElementsByTagName('table');for(var i=0;i<tables.length;i++){if(tables[i].className&&tables[i].className.indexOf('DeptDDTree')>-1){return;}}return'0|||封装提示信息：未找到cssClass为DeptDDTree的table元素，请检查是否进入了正确的iframe或者使用了正确的关键字'
    _setDropDownTree    ${deptPath}    ${locator}
